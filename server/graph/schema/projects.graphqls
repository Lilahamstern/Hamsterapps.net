type Project {
    id: ID!
    title: String!
    description: String!
    user: User!
}

input createProjectInput {
    title: String!
    description: String!
}

extend type Query {
    projects: [Project!]!
}

extend type Mutation {
    createProject(input: createProjectInput!): Project!
}